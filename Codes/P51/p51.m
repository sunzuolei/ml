clear all; close all; clc;

disp(porterStemmer('discounts'));
disp(porterStemmer('DisCounting'));
disp(porterStemmer('DisCounted'));
disp(porterStemmer('DISCOUNT'));

disp(porterStemmer('Dealer'));

disp(porterStemmer('university'));